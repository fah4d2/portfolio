# Base44 dev-only placeholders. Real external secrets are delivered by the
# platform to /run/base44/app.env (listed AFTER this file in compose env_file,
# so they always win). No real credentials live in this file.

# Google Calendar via the Lovable connector gateway is optional. When these
# are absent the site skips live free/busy checks but still renders and books.
LOVABLE_API_KEY=
GOOGLE_CALENDAR_API_KEY=
